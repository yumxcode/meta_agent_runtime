Ask the user a question, optionally with multiple-choice options.

Usage:
- question: the question to present
- options: optional list of choices (user can also provide free-form answer)
- Returns the user's response
- If no askUser callback is configured, returns a placeholder indicating human input is needed
