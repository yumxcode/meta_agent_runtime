Exit plan mode and return to normal execution.

After calling this tool all subsequent tool calls will execute immediately without requiring user approval, just as before plan mode was entered.

Only valid if plan mode is currently active; returns an error otherwise.
