Echo a message back to the caller unchanged. Returns the input text as-is.

Usage:
- text: the text to echo back (required)
- Returns `text` verbatim, with no transformation
- Use to verify the tool-call round-trip works, or to surface a computed string as a tool result
