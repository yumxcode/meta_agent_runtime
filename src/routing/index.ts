/**
 * Routing layer — explicit SessionRouter mode selection.
 */

export type {
  SessionMode,
  RouterOptions,
} from './types.js'

export { MODE_WEIGHT } from './types.js'
export { SessionRouter } from './SessionRouter.js'
