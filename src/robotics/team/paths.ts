/**
 * DEPRECATED in v2.0 — path-based module ownership was removed when team
 * mode became a collaboration log instead of a task-assignment tool.
 *
 * This file is kept as an empty module so that any stale `import` still
 * resolves; it will be removed in the next release.  Nothing here is part
 * of the public API.
 */
export {}
