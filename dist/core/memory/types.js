/**
 * Meta-Agent Memory — 类型分类与提示词文本块
 *
 * 两种记忆类型（精简设计）：
 *   user     — 用户画像：角色、背景、沟通偏好
 *   feedback — 用户对 agent 行为的纠正与确认
 *
 * Hard boundaries（全模式适用）：
 *   - 成功/失败经验、算法陷阱、调参记录 → ExperienceStore（experience_write 工具）
 *   - 工程计算结果 → ProvenanceTracker（campaign 模式）或代码注释
 *   - 活跃状态、任务进度 → campaign_context / robotics R5 section（自动注入）
 *   - 领域知识、外部 API 指针 → 项目文档或 AGENT.md，不写入 memory
 *
 * 设计原则：memory 只存"关于用户"的信息，不存"关于世界"或"关于工程"的信息。
 * 工程经验由 ExperienceStore 统一管理，具备结构化检索和跨 session 复用能力。
 */
export const MEMORY_TYPES = [
    'user',
    'feedback',
];
// ─────────────────────────────────────────────────────────────────────────────
// 前言格式
// ─────────────────────────────────────────────────────────────────────────────
export const MEMORY_FRONTMATTER_EXAMPLE = [
    '```markdown',
    '---',
    'name: {{记忆名称 — 具体且可搜索}}',
    'description: {{单行摘要，用于相关性匹配 — 尽量具体}}',
    `type: {{${MEMORY_TYPES.join(' | ')}}}`,
    'date: {{YYYY-MM-DD — 写入日期}}',
    '---',
    '',
    '{{记忆内容}}',
    '',
    '# feedback 推荐正文结构：',
    '# **规则：** 核心陈述',
    '# **原因：** 用户给出的理由或发生的事件',
    '# **适用范围：** 该规则何时生效',
    '```',
];
// ─────────────────────────────────────────────────────────────────────────────
// 类型说明节
// ─────────────────────────────────────────────────────────────────────────────
export const TYPES_SECTION = [
    '## 记忆类型',
    '',
    '仅存储以下两种类型，其他信息不进 memory：',
    '',
    '<types>',
    // ── user ──────────────────────────────────────────────────────────────────
    '<type>',
    '  <name>user</name>',
    '  <description>用户的角色、领域背景、技术深度，以及偏好的协作方式。用于校准回复的技术深度和沟通风格。</description>',
    '  <when_to_save>了解到用户角色、背景或沟通偏好时保存。</when_to_save>',
    '  <examples>',
    '    user: 我是机器人算法工程师，主要做腿式运动控制。',
    '    → [保存 user 记忆：机器人算法工程师，专攻腿式运动控制——以研究生/工程师水平校准解释]',
    '  </examples>',
    '</type>',
    // ── feedback ──────────────────────────────────────────────────────────────
    '<type>',
    '  <name>feedback</name>',
    '  <description>用户对 agent 工作方式的指导——包括要避免的和要保持的。纠正与确认都要记录。</description>',
    '  <when_to_save>用户纠正了 agent 的方法，或明确确认了某个选择有效时保存。包含原因以便判断边界情况。</when_to_save>',
    '  <body_structure>**规则：** 核心陈述。**原因：** 用户给出的理由。**适用范围：** 该规则何时生效。</body_structure>',
    '  <examples>',
    '    user: 不要在没有我明确批准的情况下合并 sub-agent 代码。',
    '    → [保存 feedback 记忆：sub-agent 代码合并前须获用户明确批准。原因：用户需要审查代码变更。]',
    '',
    '    user: 是的，这次用 squash merge 是对的。',
    '    → [保存 feedback 记忆：该用户偏好 squash merge 合并 sub-agent 分支。已确认，非纠正。]',
    '  </examples>',
    '</type>',
    '</types>',
    '',
];
// ─────────────────────────────────────────────────────────────────────────────
// 不应存入记忆的内容 — mode-specific hard boundaries
// ─────────────────────────────────────────────────────────────────────────────
export const WHAT_NOT_TO_SAVE_SECTION = [
    '## 不应存入 memory 的内容',
    '',
    'Memory 只存"关于用户"的信息。以下内容有专用系统，不得写入 memory：',
    '',
    '- **工程经验、成功/失败案例、调参记录、算法陷阱** → `experience_write`（ExperienceStore）',
    '- **仿真/计算结果** → provenance tracker（campaign 模式）',
    '- **活跃任务状态、进度** → campaign_context / robotics R5（自动注入，无需手动存储）',
    '- **领域知识、物理常数、API 指针** → 项目文档、AGENT.md 或代码注释',
    '- **对话摘要、调试步骤** → 不需要持久化',
    '',
    '违反以上边界会导致 memory 污染：不同项目、不同领域的内容混在一起，',
    '最终使 memory 对任何会话都不准确。',
    '',
];
// ─────────────────────────────────────────────────────────────────────────────
// 如何保存记忆 — 两步操作（与 CC 一致）
// ─────────────────────────────────────────────────────────────────────────────
export const HOW_TO_SAVE_SECTION = [
    '## 如何保存记忆',
    '',
    '保存记忆是两步操作：',
    '',
    '**第一步** — 将记忆写入记忆目录中的独立文件',
    '（例如 `user_role.md`、`battery_escalation_threshold.md`、`materials_internal_api.md`）：',
    '',
    ...MEMORY_FRONTMATTER_EXAMPLE,
    '',
    '**第二步** — 在 `MEMORY.md` 中添加一行指针：',
    '```',
    '- [记忆名称](filename.md) — 单行钩子，描述该文件包含的内容',
    '```',
    '',
    '`MEMORY.md` 是索引，不是内容存储——每条目应为一行，不超过约 150 个字符。',
    '不得将记忆内容直接写入 `MEMORY.md`。',
    '',
    '创建新文件前：先扫描 `MEMORY.md`，确认是否有可更新的现有条目。',
    '',
];
// ─────────────────────────────────────────────────────────────────────────────
// 何时访问记忆（含 "ignore" 处理，对齐 CC）
// ─────────────────────────────────────────────────────────────────────────────
export const WHEN_TO_ACCESS_SECTION = [
    '## 何时访问记忆',
    '',
    '- **任何任务开始前**：检查 user/feedback，校准技术深度和 agent 行为规则。',
    '- **用户询问过往方法或偏好**：检查 feedback 和 user 记忆。',
    '- **用户明确要求召回或记住某事**：立即执行。',
    '- **用户要求忽略或遗忘某条记忆**：在本次会话剩余时间内，将 MEMORY.md 视为不含该事实，不得引用或应用。',
    '',
    '工程经验（算法陷阱、成功案例、失败分析）从 ExperienceStore 加载，不在 MEMORY.md 中。',
    '',
];
// ─────────────────────────────────────────────────────────────────────────────
// 引用记忆前先验证（对齐 CC 的 TRUSTING_RECALL_SECTION，工程化适配）
// ─────────────────────────────────────────────────────────────────────────────
export const TRUSTING_RECALL_SECTION = [
    '## 引用记忆前先验证',
    '',
    'Memory 记录的是写入时刻的状态，不代表现在仍然有效：',
    '',
    '- feedback 中的规则：确认仍适用于当前上下文，用户偏好可能已变化。',
    '- user 信息：用户角色或背景可能已更新，以对话中最新信息为准。',
    '',
    '工程经验（错误模式、算法陷阱、调参结论）统一从 ExperienceStore 获取，不在 memory 中。',
    '',
];
// ─────────────────────────────────────────────────────────────────────────────
// 工程记忆漂移警告（比 CC 更强）
// ─────────────────────────────────────────────────────────────────────────────
export const DRIFT_CAVEAT = [
    '## Memory 内容已精简',
    '',
    'Memory 只包含 user 和 feedback 两类信息。',
    '工程经验、领域知识、计算结果、项目状态均不在此处——',
    '它们由 ExperienceStore、provenance tracker 和动态 context 节管理。',
    '',
];
// ─────────────────────────────────────────────────────────────────────────────
// 作用域与新鲜度元数据说明
// ─────────────────────────────────────────────────────────────────────────────
export const SCOPE_FRESHNESS_SECTION = [];
//# sourceMappingURL=types.js.map