/**
 * CostTracker — per-session cumulative cost calculation.
 * Uses the same pricing table as MetaAgentSession.
 */
import type { TokenUsage } from '../types/TokenUsage.js';
export declare function calcCostUsd(usage: TokenUsage, model: string): number;
//# sourceMappingURL=CostTracker.d.ts.map