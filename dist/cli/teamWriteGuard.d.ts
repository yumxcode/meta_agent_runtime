/**
 * DEPRECATED in v2.0 — the path-based write guard was removed when team mode
 * became a collaboration log instead of a task-assignment tool.  Coordination
 * is now signalled visually on the board (🔒 markers) and via the attempts[]
 * log, not enforced by denying tool calls.
 *
 * This file is kept as an empty module so any stale `import` still resolves;
 * the implementation will be removed in the next release.
 */
export {};
//# sourceMappingURL=teamWriteGuard.d.ts.map